import React from 'react';

import * as Styled from './index.style';

function Room(props) {
    return (
        <div>
            <Styled.StyledRoom>Room Page here</Styled.StyledRoom>
        </div>
    );
}

export default Room;
